package mx.marco.presentation.screens.pokemondescription

import mx.marco.presentation.viewstate.ViewState

data class PokemonDescriptionViewState(
    val pokemonId: Int = 0,
) : ViewState()
//ListDescription(id = 0, description = "")
